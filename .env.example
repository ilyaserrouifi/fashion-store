# Neon Database — copie l'URL depuis ton dashboard Neon (Connection String, mode "pooled")
DATABASE_URL=postgresql://user:password@ep-xxxxx-pooler.region.aws.neon.tech/dbname?sslmode=require

# JWT
JWT_SECRET=change-moi-avec-une-chaine-aleatoire-longue

# Port local (dev seulement, Vercel gère ça automatiquement en prod)
PORT=3000
